<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="MyAppTheme" parent="MyAppTheme.Base" />

    <!-- Base application theme. -->
    <!-- Customize your theme here. -->
    <style name="MyAppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="actionMenuTextColor">#323232</item>
        <item name="android:actionMenuTextColor">#323232</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_arrow_back</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="android:colorEdgeEffect" tools:targetApi="lollipop">@color/colorEdgeEffect
        </item>
    </style>

    <!-- To stylize the Hamburger icon on HomeScreen -->
    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">#323232</item>
    </style>

    <!-- To stylize the Toolbar on HomeScreen -->
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="actionOverflowButtonStyle">@style/MyOverflowButtonStyle</item>
    </style>

    <style name="MyAppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus" tools:targetApi="kitkat">true</item>
        <item name="android:windowTranslucentNavigation" tools:targetApi="kitkat">true</item>
    </style>

    <!-- To stylize the Overflow button on HomeScreen (if any) -->
    <style name="MyOverflowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">#323232</item>
    </style>

    <!-- To stylize the Scroll bar for the list on HomeScreen -->
    <style name="scrollbar_style">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarThumbVertical">@drawable/scrollable_thumb</item>
        <item name="android:scrollbarSize">4dp</item>
        <item name="android:scrollbarFadeDuration">300</item>
        <item name="android:scrollbarDefaultDelayBeforeFade">300</item>
    </style>


</resources>
